import 'package:flutter/material.dart';

class PrayerList extends StatelessWidget {
  const PrayerList({
    super.key,
    list
  });

  List<PrayerList> list;

  @override
  Widget build(BuildContext context) {
    return Flexible(
      flex: 9,
      child: ,
    );
  }
}
